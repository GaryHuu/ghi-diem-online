import { SxProps } from '@mui/material';

const styles: { [key: string]: SxProps } = {
	wrapper: {
		pt: '72px',
		pb: '100px',
	},
	content: {
		padding: '1rem',
		gap: '1rem',
	},
};

export default styles;

import { SxProps } from '@mui/material';

export const styles: { [key: string]: SxProps } = {
	wrapper: {
		justifyContent: 'center',
		alignItems: 'center',
		height: '100%',
	},
	content: {
		justifyContent: 'center',
		alignItems: 'center',
	},
};

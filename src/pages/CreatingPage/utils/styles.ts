import { SxProps } from '@mui/material';

export const styles: { [key: string]: SxProps } = {
	wrapper: {
		p: '1rem',
		height: '100%',
		justifyContent: 'center',
		alignItems: 'center',
	},
	w100: {
		width: '100%',
	},
};
